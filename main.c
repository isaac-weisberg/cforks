#include <stdio.h>
#include "queue.h"

void main() {
    Queue* q;
    // blocks_init();

    // blocks_submit(run);
    // blocks_submit(run);
    // blocks_submit(hugh);

    // blocks_submit(fun);
    // blocks_submit(fun);
    // blocks_submit(hugh);
    // blocks_submit(run);

    // blocks_main();

    printf("Program end.\n");
}


